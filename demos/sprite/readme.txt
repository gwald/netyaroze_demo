Sprite: quickstart demo to get sprites up on screen

Handles 4,8 and 16 bit TIM files.

The batch file <batch> loads program and TIM files into
the Yaroze's RAM;
program loads TIM files into VRAM (fram buffer)
and displays them on screen.
